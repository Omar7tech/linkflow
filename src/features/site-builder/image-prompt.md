# Website Builder banner

Asset: `public/tools/website-builder.webp` (1376×774, 16:9).

The file currently in the repo is a rendered placeholder, not a Gemini image: it
follows the house style (violet bloom, off-white floating panel, dot grid, soft
contact shadow) but it is flat vector rather than the semi-gloss 3D render the
rest of the set uses. Replace it when you next run the image tool.

Category is `studio`, so the hue is **violet / purple**, anchor `#8b5cf6`.

## Method A (preferred) — reference image

Attach `public/tools/vector-forge.webp` as the style reference:

> Same illustration style, composition, lighting, palette and materials as the
> reference image. Keep the identical background: pale lavender bloom behind the
> subject fading to deep violet at the corners, faint dot-grid, and soft floor
> shadow.
>
> Only change the subject on the floating panel: a webpage under construction,
> shown as clean stacked blocks — a wide hero band at the top, two short text
> bars beneath it, and a row of three equal cards below. One card is lifted a
> few millimetres out of the stack and hovers, as if being dragged into place.
> Float a small rounded cursor arrow at 10 o'clock and a tiny stack of two
> layered sheets at 2 o'clock, well clear of the panel.
>
> No text, no letters, no numbers, no logos.

## Method B — text only

> 3D semi-gloss product render, 16:9.
>
> **Layout, exactly:** hero centred, occupying 60% of frame width and 62% of
> height — a thick off-white rounded-rectangle panel tilted 5° clockwise,
> floating above a soft dark elliptical shadow on a flat floor. One rounded
> cursor arrow at 10 o'clock and one small stack of two layered sheets at
> 2 o'clock, each about 12% of frame height. Nothing else in frame.
>
> **Background, exactly:** radial gradient, pale lavender-white `#e9e2ff` at
> centre directly behind the panel, to `#5b21b6` at the four corners. Faint
> dot-grid fading in at the left and right edges only, never over the panel.
>
> **Colour, exactly two materials:** matte off-white `#f4f4f5` and semi-gloss
> violet `#8b5cf6`. No third colour anywhere.
>
> **On the panel:** a page laid out in blocks — a wide violet hero band across
> the top, two short light text bars, and a row of three cards, with one card
> lifted out of the row and hovering slightly above its slot.
>
> **Lighting:** one soft key from the upper left, gentle fill, soft shadows. No
> rim light, no glow, no neon, no lens flare, no depth of field.
>
> No text, no letters, no numbers, no logos, no brand marks.

**Negatives:** text, letters, numbers, labels, watermark, four-point sparkle,
star, sparkle glyph, logos, brand marks, dark background, black background,
neon, glow, bloom haze, cyberpunk, rainbow, multicolour, teal, orange, busy
background, cluttered layout, gradient banding, depth of field, blurry, bokeh,
lens flare, harsh reflections, realistic human hands, low quality, distorted
shapes.
